const studentsArr: string[] = ["Ahmet", "Mehmet", "Ali", "Emin", "Furkan"];

const poped = studentsArr.pop(); //Removes the last element from an array and returns it. If the array is empty, undefined is returned and the array is not modified.

console.log("poped", poped); // output -> Furkan
